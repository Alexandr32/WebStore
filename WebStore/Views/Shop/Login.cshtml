
@{
    ViewData["Title"] = "Login";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<section id="form">
    <!--form-->
    <div class="container">
        <div class="row">
            <div class="col-sm-4 col-sm-offset-1">
                @await Html.PartialAsync("Partial/_LoginForm")
            </div>
            <div class="col-sm-1">
                <h2 class="or">OR</h2>
            </div>
            <div class="col-sm-4">
                @await Html.PartialAsync("Partial/_SignUpForm")
            </div>
        </div>
    </div>
</section><!--/form-->
