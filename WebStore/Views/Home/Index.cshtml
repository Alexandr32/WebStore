
@{
    ViewBag.Title = "Индекс";
}

@* Слайдер *@
@await Html.PartialAsync("Partial/_Slider")

<section>
    <div class="container">
        <div class="row">
            <div class="col-sm-3">
                @* Левое меню *@
                @await Html.PartialAsync("Partial/LeftSideBar/_LeftSideBar")
            </div>

            <div class="col-sm-9 padding-right">
                @*@await Html.PartialAsync("Partial/Catalog/_FeaturesItems")*@

                @await Html.PartialAsync("Partial/Catalog/_CategoryTab");

                @await Html.PartialAsync("Partial/Catalog/_RecommendedItems");

            </div>
        </div>
    </div>
</section>

